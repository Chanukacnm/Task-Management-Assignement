/**
 * Default (development) environment settings, used by `ng serve`.
 * Change `apiBaseUrl` to point the Angular client at a different API host/port.
 * For production builds this file is replaced by environment.production.ts.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5142/api'
};
