/**
 * Production environment settings (used by `ng build`, via fileReplacements).
 * Update `apiBaseUrl` to the deployed API URL before a real production build.
 */
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:5142/api'
};
